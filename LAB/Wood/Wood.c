#include <stdio.h>



int main ( void ) {
    int num_tests;
    scanf("%d",&num_tests);
    for (int i = 0 ; i < num_tests; i++) {
        char *name;
        char **names;
        while (scanf("%s",name) != EOF) {
            
        } 
    }
}